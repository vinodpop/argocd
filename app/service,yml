apiVersion: v1
kind: Service
metadata:
    name: argocd-svc
    namespace: privatecloud
spec:
    type: LoadBalancer
    selector:
        app: argocd-app
    ports:
        - protocol: TCP
          port: 80
          targetPort: 5000

